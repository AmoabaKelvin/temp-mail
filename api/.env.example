# Server Configuration
ADDR=localhost:8080  # Change this if running on a different port

# Database Configuration (Local)
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=temp_mail
DB_SSLMODE=disable

# Connection Pool Settings
DB_MAX_OPEN_CONNS=10
DB_MAX_IDLE_CONNS=5
DB_MAX_IDLE_TIME=5m

# Database Connection String (for remote deployment, e.g., Railway, Render, or Supabase)
# Uncomment and configure if using a remote PostgreSQL database
# DB_ADDR=postgresql://your_db_user:your_db_password@your_db_host:your_db_port/your_db_name

# Example: DB_ADDR=postgresql://user:password@db.example.com:5432/temp_mail
